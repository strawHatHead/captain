import React from 'react';
import {
 Card
} from '@alifd/next';
import styles from './index.module.scss';

export default function Base() {
  const data = [
    
  ];
  return (
    <Card className={styles.Base}>
      <div className={styles.Base_title}>基础知识</div>
    </Card>
  );
}
